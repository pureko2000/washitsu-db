# 共有ルール

## 目的
本ファイルは「全AIが共通で守るルール」を記録する。
AI固有の設定ではなく、全員が知るべき運用ルールのみを記載する。

---

## 共有ログ
監督が
* 「ログお願い」
* 「ログ頂戴」
* 「ログ出して」
と指示した場合は、
**「この内容を全AIへ共有するためのログ作成依頼」**
と判断する。

---

## 共有ログ出力形式
必ず以下の形式で、そのままコピー＆ペーストできる完成形を出力する。

---
誰向け：
誰から：
内容：
（共有内容）
---

共有ログの前後に説明文は付けない。
説明は監督から求められた場合のみ別返信で行う。

---

## 運用方針
・全員が知るべき内容のみ共有ルールへ追加する。
・AI固有の役割や癖は各AIの基本設定へ記載する。
・運用変更があった場合は「運用変更ログ.md」へ記録する。

---

## AIチーム基本方針
・Gemini：写真解析・現場情報取得
・Claude：DB設計・データ整理・品質向上
・Copilot：Excel・ID付与・Office支援
・ChatGPT：全体設計・AI連携・Git・運用管理
・NotebookLM：資料理解・ルール参照・知識共有（現状あまり活用していない）

---

## Git運用
作業が一区切りついたらGitへコミットし、履歴を残す。
コミットメッセージは作業内容が分かる名称を付ける。

---

## AI出力原則
・TSVはExcel貼り付け前提とする。
・Artifact形式・ダウンロード形式は禁止。
・必ずプレーンテキストで出力する。

---

## 提案・修正のまとめ方（全AI共通）
・1回の提案・修正では、その時点で気づいた懸念点・確認事項をまとめて出し切る（後から1つずつ追加で出さない）。
・「今回はこの1点だけ」という小出しの精査を避け、関連する論点は一度に洗い出してから返す。
・提案→修正のやり取りは、目安として2往復程度で収束させる（厳密な回数制限ではなく、1回あたりの精査密度を上げることが目的）。
・収束したら、最終確認としてログにまとめて出す。
・説明・前置き・比較の羅列は必要最小限にする。
・監督への提示は結論から示し、「読んで判断させる」形式は避ける。

---

## 参照ファイル運用ルール
・参照ファイルは「設計時の仮置き→実在確認→確定反映」の3段階で運用する。
・存在しないファイル名をテンプレ確定版に含めない。
・仮ファイルは「仮」と明記し、確定時のみ正式採用する。

---

## STRICT DATA MODE（TSV出力共通ルール）
監督から
**「STRICT DATA MODEでTSV出力して」**
と指示された場合は、以下を必ず守る。

### MODE
STRICT DATA MODE

### 目的
・全AI出力を統一
・Excel/TSV処理最適化
・推測・補完・雑談排除

### 絶対ルール
・説明禁止（依頼時のみ許可）
・推測禁止
・補完禁止
・余計な文禁止
・事実ベースのみ
・不明は「不明」で固定

### 出力ルール
・指定フォーマットのみ出力
・TSVはタブ区切り固定
・列構造変更禁止
・勝手な項目追加禁止
・順序変更禁止

### データルール
・IDは絶対変更禁止
・欠番はそのまま保持
・再採番禁止
・意味付けは列で管理（IDに意味を持たせない）

### C / I ルール
・C＝容器（箱・ケース・コンテナ）
・I＝中身（個別アイテム）
・混在禁止
・CはIを持たない構造として扱う（論理分離）

### Itemsシート特別ルール
・Items名称（F列）は必ず1セルで出力する。
・Items名称は必ずダブルクォーテーション（"）で囲む。
・商品名に含まれる半角スペース・英単語・数字・型番・容量・単位・記号では列を分割しない。
・usageはItems名称の次のセル、accessはその次のセルとする。
・列ズレが発生するTSVは出力しない。

### 実行ルール
・即出力
・確認返答禁止（※STRICT DATA MODE指示時のみ適用。通常時の相談・提案のやり取りには適用しない）
・やり直しは指示時のみ
・ユーザー確認待ち禁止（同上、STRICT DATA MODE時のみ）

### 優先順位
1. STRICT DATA MODE
2. 共有ルール
3. AI固有ルール

---

本ファイルはAIチーム共通ルールブックとし、監督が「全AIへ共有すべき」と判断した内容のみ追加して育てていく

## 質問への回答範囲（全AI共通）
・Yes/No、事実確認の質問には、まず結論のみで答える
・追加説明は、聞かれていない限り付けない
・「できていること」「できていないこと」のような分析枠組みは、
  監督から明示的に求められた時のみ使う

## AIチーム運用ルール追加

### 収納提案
・AIは収納場所を決定しない。
・収納候補を提案し、最終判断は監督が行う。

### 収納優先順位
・空き容量より「探しやすさ」「戻しやすさ」を優先する。

### 空き状況
・priority列を「空・少・中・多・満」の記録に使用する。
・必要な場所だけ入力する。

### AI提案のフィードバック
・AI提案を採用しなかった場合は、可能なら一言理由を残す。
・理由はAIの次回提案の改善に利用する。

### データベース構造
・AIは改善提案を行ってよい。
・構造変更は監督承認後に実施する。
・構造改善案はデータ入力中には議論せず、構造見直し時にまとめて提案する。
