誰向け：共有
誰から：くろ（Claude）
内容：

共有ルール.md「## STRICT DATA MODE（TSV出力共通ルール）」の節を
以下の内容に差し替えてください（既存の同名セクションを丸ごと置換）。

---
## STRICT DATA MODE（TSV出力共通ルール）
監督から
**「STRICT DATA MODEでTSV出力して」**
と指示された場合は、以下を必ず守る。

### MODE
STRICT DATA MODE

### 目的
・全AI出力を統一
・Excel/TSV処理最適化
・推測・補完・雑談排除

### 絶対ルール
・説明禁止（依頼時のみ許可）
・推測禁止
・補完禁止
・余計な文禁止
・事実ベースのみ
・不明は「不明」で固定

### 出力ルール
・指定フォーマットのみ出力
・TSVはタブ区切り固定
・列構造変更禁止
・勝手な項目追加禁止
・順序変更禁止

### データルール
・IDは絶対変更禁止
・欠番はそのまま保持
・再採番禁止
・意味付けは列で管理（IDに意味を持たせない）

### C / I ルール
・C＝容器（箱・ケース・コンテナ）
・I＝中身（個別アイテム）
・混在禁止
・CはIを持たない構造として扱う（論理分離）

### Itemsシート特別ルール
・Items名称（F列）は必ず1セルで出力する。
・Items名称は必ずダブルクォーテーション（"）で囲む。
・商品名に含まれる半角スペース・英単語・数字・型番・容量・単位・記号では列を分割しない。
・usageはItems名称の次のセル、accessはその次のセルとする。
・列ズレが発生するTSVは出力しない。

### 実行ルール
・即出力
・確認返答禁止（※STRICT DATA MODE指示時のみ適用。通常時の相談・
　提案のやり取りには適用しない）
・やり直しは指示時のみ
・ユーザー確認待ち禁止（同上、STRICT DATA MODE時のみ）

### 優先順位
1. STRICT DATA MODE
2. 共有ルール
3. AI固有ルール
---
誰向け：共有
誰から：くろ（Claude）
内容：

共有ルール.mdへ、以下の項目を追記してください。

---

## 提案・修正のまとめ方（全AI共通）
- 1回の提案・修正では、その時点で気づいた懸念点・確認事項を
  まとめて出し切る（後から1つずつ追加で出さない）
- 「今回はこの1点だけ」という小出しの精査を避け、関連する論点は
  一度に洗い出してから返す
- 提案→修正のやり取りは、目安として2往復程度で収束させる
  （厳密な回数制限ではなく、1回あたりの精査密度を上げることが
  目的）
- 収束したら、最終確認としてログにまとめて出す
- 説明・前置き・比較の羅列は必要最小限にする
- 監督への提示は結論から示し、「読んで判断させる」形式は避ける

---

以上、追記をお願いします。
補足：本モードは「STRICT DATA MODEでTSV出力して」と明示された
場面にのみ適用され、通常の相談・提案ベースのやり取りを妨げるもの
ではありません。
